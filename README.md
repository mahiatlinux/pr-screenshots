PR #11301 verification evidence

See commands.txt for SHAs, environment, executed commands and fixture boundaries. All screenshot comparisons were manually inspected. Checkpoint fixtures exercise real provenance, filesystem validation, history API and UI; they are not GPU training runs.
